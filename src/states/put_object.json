{
  "PutObject": {
    "Type": "Task",
    "End": true,
    "Parameters": {
      "Body.$": "$.analyze",
      "Bucket": "${result_bucket}",
      "Key.$": "States.Format('result/{}', States.ArrayGetItem(States.StringSplit($.detail.object.key, 'upload/'), 1))"
    },
   "Resource": "arn:aws:states:::aws-sdk:s3:putObject"
  }
}
